config {
    type: "incremental"
}

SELECT distinct ProjectId,ParentId,childId,ParentMobile,CallstartDatetime
from ${ref("incoming")}
Union distinct

SELECT distinct ProjectId,ParentId,childId, ParentMobile,CallstartDatetime
from ${ref("roster")}
